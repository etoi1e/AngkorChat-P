<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Auth.PasswordActivity">

    <View
        android:id="@+id/viewUnderLinePassword"
        android:layout_width="0dp"
        android:layout_height="2dp"
        android:background="#dadde3"
        app:layout_constraintBottom_toBottomOf="@+id/viewNumberBoxPassword"
        app:layout_constraintEnd_toEndOf="@+id/viewNumberBoxPassword"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="@+id/viewNumberBoxPassword" />

    <View
        android:id="@+id/viewNumberBoxPassword"
        android:layout_width="0dp"
        android:layout_height="52dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/tvPasswordPassword"
        app:layout_constraintTop_toBottomOf="@+id/tvPasswordPassword" />

    <ImageView
        android:id="@+id/imgMoveBackPassword"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="14dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_arrow_left_line" />

    <TextView
        android:id="@+id/tvLoginPassword"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Login"
        android:textColor="@color/textBlack"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/imgMoveBackPassword" />

    <TextView
        android:id="@+id/tvPasswordPassword"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:text="Password"
        android:textColor="@color/textBlack"
        app:layout_constraintStart_toStartOf="@+id/imgMoveBackPassword"
        app:layout_constraintTop_toBottomOf="@+id/tvLoginPassword" />

    <EditText
        android:id="@+id/etPasswordPassword"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:ems="10"
        android:inputType="textPassword"
        app:layout_constraintBottom_toBottomOf="@+id/viewNumberBoxPassword"
        app:layout_constraintStart_toStartOf="@+id/viewNumberBoxPassword"
        app:layout_constraintTop_toTopOf="@+id/viewNumberBoxPassword" />

    <ImageView
        android:id="@+id/imgDeletePassword"
        android:layout_width="20dp"
        android:layout_height="20dp"
        app:layout_constraintBottom_toBottomOf="@+id/viewNumberBoxPassword"
        app:layout_constraintEnd_toEndOf="@+id/viewNumberBoxPassword"
        app:layout_constraintTop_toTopOf="@+id/viewNumberBoxPassword"
        app:srcCompat="@drawable/ic_delate_circle_fill_lightgray" />

    <TextView
        android:id="@+id/tvForgotPassword"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Forgot Password?"
        android:textColor="@color/textBlack"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@+id/btnLoginPassword"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/viewUnderLinePassword" />

    <android.widget.Button
        android:id="@+id/btnLoginPassword"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="36dp"
        android:background="@drawable/style_disable_btn"
        android:clickable="false"
        android:text="Login"
        android:textAllCaps="false"
        android:textColor="@color/white"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>